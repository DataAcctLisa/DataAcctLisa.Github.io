#countdown program that can be used as a timer


import time

number_as_string = input("Enter number of seconds you want to count down:  ")
number_as_integer = int(number_as_string)

count = number_as_integer

while count > 0:
    print (count)
    time.sleep(1)
    count = count - 1
print ('dinner\'s ready')
